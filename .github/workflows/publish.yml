#publish your docker image to github container registry
name: publish
on: [push]
jobs:
  publish-first-dockerimage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build the first-docker Docker image
        run: |
          docker build . --tag ghcr.io/ceewa30/
          docker run ghcr.io/ceewa30/first-docker-gcr:latest
          docker push ghcr.io/ceewa30/first-docker-gcr:latest
